{
  "name": "sprout",
  "version": "0.1.0",
  "description": "Sprinkler layout sandbox — emergent design through animation",
  "scripts": {
    "dev": "bun --hot index.html",
    "build": "bun build src/main.ts --outdir dist --target browser",
    "serve": "bunx serve dist"
  },
  "devDependencies": {}
}
